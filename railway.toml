[build]
builder = "nixpacks"

[deploy]
startCommand = "waitress-serve --host=0.0.0.0 --port=$PORT app:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
REMATES_DATA_DIR = "/data"
